# Ignore the .terraform directory (used for provider plugins, etc.)
terraform/.terraform/

# Ignore Terraform lock and state files
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.*

# Ignore sensitive variable files (optional — only if you use them)
terraform/*.tfvars
terraform/*.tfvars.json
terraform/secrets.tfvars

# Ignore Terraform plan output files
terraform/*.tfplan

# Optional: Crash logs (in case Terraform ever generates them)
terraform/crash.log
